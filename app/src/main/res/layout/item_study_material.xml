<!-- res/layout/item_study_material.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="16dp">

    <!-- CardView to create a box-like appearance -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:elevation="4dp"
        android:background="@drawable/card_background">

        <!-- Use LinearLayout to arrange TextViews vertically -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="8dp">

            <!-- Add necessary UI elements to display study material details -->
            <TextView
                android:id="@+id/studyMaterialTitleTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="18sp"
                android:textStyle="bold"
                android:layout_marginBottom="4dp"/>

            <TextView
                android:id="@+id/studyMaterialDescriptionTextView"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textSize="16sp"
                android:layout_marginTop="4dp"
            android:layout_marginBottom="8dp"/>

            <!-- Add more TextViews, ImageViews, or other UI elements as needed -->

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</RelativeLayout>
